# simple_interest.py

# Define the variables for Principal (P), Rate (R), and Time (T)
principal = 1000
rate = 0.05
time = 3

# Calculate the simple interest using the formula: I = P * R * T
interest = principal * rate * time

# Print the result in the specified human-readable format
print(f"The simple interest is: {interest}")